server {
    listen 80;
    
    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        # Redirecționează orice rută către index.html (SPA support)
        try_files $uri $uri/ /index.html;
    }

    error_page  404 500 502 503 504 /index.html;
    }